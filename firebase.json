{
  "functions": [
    {
      "source": "functions",
      "codebase": "dataconnect", 
      "ignore": [
        "node_modules",
        ".git",
        "firebase-debug.log",
        "firebase-debug.*.log",
        "*.local"
      ],
      "predeploy": [
        "cmd /c npm --prefix \"$RESOURCE_DIR\" run lint",
        "cmd /c npm --prefix \"$RESOURCE_DIR\" run build"
      ]
    }
  ]
}
