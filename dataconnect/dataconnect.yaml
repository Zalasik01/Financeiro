specVersion: "v1"
serviceId: "financeiro"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "financeiro-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
